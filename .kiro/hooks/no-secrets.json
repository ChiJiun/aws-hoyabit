{
  "version": "v1",
  "hooks": [
    {
      "name": "金鑰洩漏掃描",
      "trigger": "PostFileSave",
      "description": "任何程式或設定變更後掃描是否寫死機密",
      "matcher": "(lambda[\\\\/].*\\\\.py|frontend[\\\\/].*\\\\.html|scripts[\\\\/]|\\.env\\.example)$",
      "action": {
        "type": "agent",
        "prompt": "掃描剛修改的檔案是否含有寫死的 API 金鑰、AWS credentials、bucket 名稱以外的機密(長隨機字串、AKIA 開頭、sk- 開頭、Bearer token 等)。機密只能從環境變數讀取(經由 config.py)。.env.example 只能含變數名與佔位值。若發現疑似洩漏,立即指出位置並改為環境變數讀取,同時提醒:若已 commit 過需視為已洩漏、應輪替金鑰。"
      }
    }
  ]
}